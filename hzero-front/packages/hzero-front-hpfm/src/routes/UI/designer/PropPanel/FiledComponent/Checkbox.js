/**
 * FieldCheckbox.js
 * @author WY
 * @date 2018/11/14
 * @email yang.wang06@hand-china.com
 */

import React from 'react';
// import { Form, Checkbox as HzeroCheckbox, Input } from 'hzero-ui';

// import intl from 'utils/intl';

export default class FieldCheckbox extends React.Component {
  render() {
    // const { form, propValues = {} } = this.props;
    return <React.Fragment />;
  }
}

export function getConfigOfPropValues(propValues, newConfig = []) {
  return newConfig;
}

export const lifeUpgrade = [];

FieldCheckbox.getConfigOfPropValues = getConfigOfPropValues;

FieldCheckbox.lifeUpgrade = lifeUpgrade;
